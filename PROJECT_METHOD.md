# 从需求到可维护的小项目：实践方法

这份方法来自 Task Tracker 的实际过程。目标不是一开始设计出“完美架构”，而是让一个空项目可以逐步变成正确、可测试、能维护的程序。

## 0. 先决定：这个项目要练什么

每个小项目最多选一到两个主要训练目标。Task Tracker 的重点是：命令行输入、文件持久化、数据建模、错误处理和模块拆分。

不要同时强迫自己学习新构建系统、复杂框架、网络、数据库、测试框架和陌生算法。那会让你无法分辨自己究竟卡在哪一层。

## 1. 把自然语言需求改写为可执行清单

不要直接开始写 `main`。先把需求翻译成表格：

| 功能 | 输入 | 成功结果 | 需要修改的数据 | 错误情况 |
| --- | --- | --- | --- | --- |
| add | 描述 | 新任务及 id | 新增任务 | 缺少/空描述、非法字符、保存失败 |
| update | id、描述 | 描述被更新 | description、updatedAt | id 非法/不存在、描述非法 |
| delete | id | 任务被移除 | 删除任务 | id 非法/不存在、保存失败 |
| mark-done | id | 状态变为 done | status、updatedAt | id 非法/不存在 |
| list | 可选 status | 打印任务 | 无 | status 非法 |

这张表就是最初级但非常有效的设计。它迫使你在写代码前区分：输入、输出、状态变化和错误。

## 2. 先回答数据问题，再回答函数问题

对任何项目，先问：程序需要在运行期间记住什么？程序结束后需要保存什么？

Task Tracker 的答案是：

```text
Task = id + description + status + createdAt + updatedAt
任务列表 = vector<Task>
持久化文件 = tasks.json
```

再写出不变量（始终应成立的规则）：

- 每个 id 是正整数且唯一。
- status 只能是三种已定义状态。
- 创建后 `createdAt` 不再改变。
- 对任务的修改必须更新 `updatedAt`。
- 只有数据成功保存后，才报告操作成功。

不变量会帮助你找 bug：如果结果不对，就检查是哪条规则被破坏了。

## 3. 用数据流画出程序骨架

先不考虑每行语法，只写一条数据流：

```text
命令行参数
  ↓
验证与解析
  ↓
内存中的数据模型
  ↓
业务操作（新增、修改、删除、查询）
  ↓
持久化存储 / 终端输出
```

Task Tracker 还有一条启动/结束的数据流：

```text
tasks.json → loadTasks → vector<Task> → 命令执行 → saveTasks → tasks.json
```

当你不知道函数该放哪里时，先问它在这条流中的位置。负责“读文件”的函数不应同时决定某个命令是否合法；负责“更新状态”的函数不应理解 JSON 标点。

## 4. 最小可运行版本：一次只打通一条路径

推荐实现顺序：

1. 空 `main` 能编译、运行、打印参数。
2. 只识别 `add` 命令并检查参数数量。
3. 建立 `Task` 和 `vector<Task>`，在内存中添加并打印任务。
4. 实现保存，再实现加载。
5. 实现 `list`，确认重启程序后数据仍存在。
6. 一个一个增加 update、状态变更、delete。
7. 最后补错误输入、整理模块和 README。

每一步都要能运行。这样出错时，你知道问题来自“刚添加的这一小步”，而不是来自几百行尚未验证的代码。

## 5. 从“功能”拆成“函数职责”

函数不是越多越好；判断标准是能否用一句话说清它的职责。Task Tracker 的好例子：

- `parseId`：把文本转换为合法的正整数 id。
- `findTaskById`：在任务列表中查找对应任务。
- `getNextId`：根据现有任务计算下一个 id。
- `isSupportedDescription`：判断描述是否能被当前 JSON 方案安全保存。
- `loadTasks`：读取并解析任务文件。
- `saveTasks`：把任务列表写成文件。
- `printTask`：以统一格式输出一条任务。

如果一个函数同时做“读取参数、找任务、修改任务、写 JSON、打印成功”，它会难以测试和修改。先把其中重复或独立的动作抽出即可；不必为了“面向对象”强行创建许多类。

## 6. 何时拆分模块

一个实用规则：当两个部分的修改原因不同，就考虑分文件。

本项目中：

- 修改任务字段或状态映射时，关注 `Task.h/.cpp`。
- 修改 JSON 格式或文件读写时，关注 `Storage.h/.cpp`。
- 修改命令使用方式时，关注 `main.cpp`。

这就是模块边界。它不是按“代码行数”拆，而是按职责拆。

### 头文件与实现文件的约定

- `.h`：类型定义、函数声明、其他文件需要知道的接口。
- `.cpp`：函数实现、只在本文件使用的辅助细节。
- 头文件只包含自己声明所必需的头文件，不依赖“别的头文件碰巧已经包含了什么”。

例如 JSON 的 `extractStringField`、`parseTask` 是 `Storage.cpp` 的内部细节，不应放进 `Storage.h`。其他模块只需知道 `loadTasks` 和 `saveTasks`。

## 7. 可维护性的五条基本规则

### 7.1 让名字回答“这是什么/做什么”

`parseId`、`findTaskById`、`updatedAt` 比 `func1`、`x`、`time2` 更容易在数周后读懂。变量名不必过长，但应避免模糊。

### 7.2 每层只处理自己的错误

- `parseId` 负责“文本不是合法 id”。
- `loadTasks` 负责“文件打不开或 JSON 不合法”。
- 命令逻辑负责“该 id 对应的任务不存在”。
- `saveTasks` 负责“写入失败”。

这样错误消息接近原因，也减少了重复判断。

### 7.3 避免复制粘贴后慢慢分叉

`mark-done` 与 `mark-in-progress` 当前相似是可接受的；当相似代码开始经常一起修改，或新增更多状态操作时，再提取一个“按 id 更新状态”的函数。重构的时机是“重复已经造成维护成本”，不是刚看到两段相似代码时。

### 7.4 区分“临时调试输出”和正式输出

调试时打印 `argv`、JSON 片段、字段值非常有用。确认功能后，应删除或注释临时输出，保留清晰的用户提示和错误信息。正式输出也是命令行程序的接口。

### 7.5 把限制写出来

当前手写 JSON 解析器不支持任意转义字符串，因此限制描述不能含某些字符。把这一点写进 README 和错误信息，比假装所有输入都能支持更可靠。

## 8. 错误处理不是最后再补的功能

为每条命令至少测试四类情况：

1. 正常输入。
2. 参数缺失或多余。
3. 格式非法，例如 `abc` 作为 id。
4. 合法但不存在，例如不存在的 id。

涉及文件时再补：文件不存在、文件为空、文件损坏、无法保存。对于 Task Tracker，文件不存在是可恢复状态，应创建 `[]`；文件内容损坏则应停止操作，避免覆盖原始数据。

## 9. 设计“完成”的测试，而不只是运行一次

在动手前或实现途中写一个最小验收清单：

```text
[ ] add 后 list 能看到新任务
[ ] 重启程序后 list 仍能看到任务
[ ] update 只改变 description 与 updatedAt
[ ] mark-done 后 list done 能找到该任务
[ ] delete 后任务消失
[ ] 非法输入不修改 tasks.json
[ ] tasks.json 不存在时会自动创建
[ ] 损坏的 tasks.json 不会被静默覆盖
```

“功能写完”应当意味着这张清单可重复通过，而不只是某次命令没有报错。

## 10. Git 是项目过程的一部分

建议在具有独立意义的小阶段提交：

```text
Initialize task tracker CLI
Add task model and command parsing
Persist tasks to JSON
Implement task update and status commands
Split storage into modules
Improve README documentation
```

提交不是“备份按钮”，而是可阅读的开发历史。提交前看 `git status`；只 `git add` 本次有意修改的文件；不要提交可重新生成的可执行文件或个人数据文件。

## 11. 与 AI 协作的推荐流程

你目前最应该保留的困难是需求拆分、数据设计、函数职责和初步调试。对此采用以下流程：

1. **20 分钟无 AI 设计。** 写功能表、数据、不变量、函数草图和错误情况。
2. **自己完成一个最小功能。** 允许查语法资料，但不要求 AI 生成完整实现。
3. **卡住时缩小问题。** 说明输入、预期、实际输出和已尝试内容，请 AI 给提示或解释。
4. **自己实现后再 review。** 请 AI 检查边界、命名、重复逻辑和测试遗漏。
5. **复盘一个关键卡点。** 记录“我原先怎么想、实际问题是什么、下次如何更快定位”。

可以交给 AI 的低价值摩擦包括 README 排版、重复性配置、查找 API、机械重构和已有设计后的样板代码。不要把完整架构和核心逻辑的第一次尝试直接外包。

## 12. 下一个项目的开工模板

复制下面内容到新项目的 `DESIGN.md`，先自己填完再写代码：

```markdown
# 项目名称

## 目标
- 这个程序为谁解决什么问题？

## 功能清单
- [ ] 功能 1：输入 / 输出 / 错误情况
- [ ] 功能 2：输入 / 输出 / 错误情况

## 数据模型
- 程序运行时要保存什么？
- 重启后仍需保存什么？存在哪里？
- 哪些规则必须始终成立？

## 数据流
输入 → 验证 → 内存数据 → 操作 → 输出/存储

## 函数或模块草图
- 模块/函数 A：负责什么？不负责什么？
- 模块/函数 B：负责什么？不负责什么？

## 验收测试
- 正常路径：
- 缺少参数：
- 非法格式：
- 合法但不存在：
- 文件/持久化异常：
```

填完并不意味着设计永远不能改。设计的价值是让你带着假设开始；实现和测试会告诉你哪些假设需要调整。

## 13. 下一次可以刻意练习什么

下一个小项目不必更大。优先选择仍能练到“输入 → 数据 → 操作 → 保存/输出”完整循环、但数据模型略有变化的题目。开工时先自己写 `DESIGN.md`，并把“需求与模块草图”作为你必须亲自拥有的部分。

当你能连续完成几次这样的设计—实现—测试—复盘循环，再考虑让 AI 根据你的设计实现部分细节。目标不是拒绝 AI，而是始终拥有判断 AI 输出是否正确的能力。
